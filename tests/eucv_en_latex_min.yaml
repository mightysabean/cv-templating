# Minimal config file for test.
# Must contain the info used on the template.jinja.
# Path of files must be relative to base_dir directory (see below config section).
# Please, avoid comments between variables, especially in list and arrays.
# This is a YAML file. Please, see xxx to get an understanding of the format.
# In the text tutorial are explained the main things you must be aware.

config:
  base_dir: '' # you must change this to the directory where you have your data and templates structure
  output_dir: 'output/latex'
  template_base_dir: ''
  template_file: 'min_test.tex' # must be relative to template_base_dir
  template_type: latex
  output_filename: "Someone"
  arara: true
  resources: # Will be copied to de tmp dir inside the directories specified by name of variable:
    build: # Will be copied to de tmp dir ONLY. They are resources needed to build the document:
      figures:
        - "../examples/data/figures/bobo.png" # Relative to base directory
      build_directory: # to be copied to the same folder where the template document will build
    #output: # Will be copied to de output folder inside a directory with the name as the variable

doc:
  doc_options: "english,a4paper,nologo"
  pdftitle: "Curriculum vitae"
  bigitem:
    - "Personal statement"  # In Europass could be 'Job applied for', 'Position', 'Preferred job', 'Studies applied for'
    - "Something good about Bobo."
data:
  persinfo: "pers_info-en_min.yaml"

